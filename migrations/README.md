# migrations

Reserved for PostgreSQL schema migrations once the server moves from
its current in-memory `openparts-data` index (see `src/store.rs`) to a
real Production Database (Architecture Specification section 22-25).
Empty for now -- no database exists yet.
