# docs

Server-specific notes go here. The full specification set (Architecture,
Canonical Data, Agent Ingestion, Testing and Quality) lives in the
sibling `openparts` repository's `docs/spec/` directory, mirrored from
the top-level project `docs/`.
